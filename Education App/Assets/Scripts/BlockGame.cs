using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BlockGame : MonoBehaviour
{
    private BuildBlock[] buildBlocks; // contains all building block references
    private MatchPair[] studySet; // contains all the match pairs in this session
    private MatchPair[] pendingSet; // contains all the match pairs that are waiting to be displayed
    public int numOfBuildBlocks;


    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
